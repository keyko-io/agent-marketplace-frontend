/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "loremflickr.com",
      "picsum.photos",
      "avatars.githubusercontent.com",
    ],
  },
}

export default nextConfig
